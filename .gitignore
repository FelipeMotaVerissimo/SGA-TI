node_modules/
.env
.env.test

# ---------------------------------------------------------------------------
# Ambiente pessoal do Endrik — NAO versionar.
#
# Nada aqui faz parte do sistema entregue: e so ferramenta local para rodar e
# validar o projeto sem MySQL. Fica fora do repositorio para nao poluir o
# codigo do TCC e para nao quebrar no clone de outra pessoa.
#
# Regra: nenhum arquivo versionado (package.json, docs/, tests/) pode
# depender de algo desta lista.
# ---------------------------------------------------------------------------
sandbox/
setup-dev.cmd
rodar.cmd
CLAUDE.md

# Bancos SQLite e schema derivado vivem em sandbox/prisma-dev/, ja coberto
# acima. As linhas abaixo pegam sobras de execucoes antigas.
prisma/dev.db
prisma/dev.db-journal
prisma/test.db
prisma/test.db-journal
prisma/schema.dev.prisma

# prisma/migrations/ NAO entra aqui: as migrations de MySQL sao parte da
# entrega. Sem elas ninguem recria o banco a partir do repositorio.

# Configuracao local do Claude Code (preview/launch) — NAO versionar.
.claude/
