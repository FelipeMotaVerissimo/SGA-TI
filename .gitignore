node_modules/
.env
.env.test

# Banco de desenvolvimento (SQLite) — artefatos gerados, não versionar
prisma/dev.db
prisma/dev.db-journal
prisma/schema.dev.prisma

# Capturas geradas pelo ambiente de preview
sandbox/telas/

# ATENÇÃO: prisma/migrations/ já vinha ignorado no repositório original.
# Para o TCC, as migrations DEVEM ser versionadas — sem elas o Felipe não
# consegue recriar o banco a partir do repositório. Combine com ele antes
# de remover a linha abaixo.
prisma/migrations/
